name: aqniet_app
description: "AQNIET Android application based on WordPress WebView."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # iOS style icons
  cupertino_icons: ^1.0.8

  # WebView (сайт внутри приложения)
  webview_flutter: ^4.7.0

  # Открытие внешних ссылок (tel:, mailto:, https:)
  url_launcher: ^6.3.0

  # 🔐 PIN AppLock
  flutter_secure_storage: ^9.0.0   # безопасное хранение PIN
  pinput: ^5.0.0                   # ввод PIN-кода
  crypto: ^3.0.3                   # хэш PIN (не храним в открытом виде)

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # Генерация иконки приложения
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/icon.png

flutter_icons:
  android: true
  image_path: assets/icon.png
